apiVersion: v1
kind: Pod
metadata:
  name: {{ .Values.podName }}
  labels:
{{ include "toYaml" .Values.labels | indent 2 }}
spec:
  containers:
    - name: nginx
      image: {{ .Values.image_name }}
      ports:
      {{- range .Values.ports }}
      - containerPort: {{ . }}
      {{- end }}

      env:
      {{- range $k, $v := .Values.env }}
      - name: {{ $k }}
        value: {{ $v }}
      {{- end }}

